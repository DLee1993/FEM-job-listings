import desktop from "../images/bg-header-desktop.svg";
const Header = () => {
    return (
        <header className="bg-primary-DarkCyan">
            <img src={desktop} alt="banner" />
        </header>
    );
};

export default Header;
